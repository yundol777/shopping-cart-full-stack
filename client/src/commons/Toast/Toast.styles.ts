import styled from "@emotion/styled";

export const Container = styled.div`
  position: absolute;
  right: 24px;
  bottom: 84px;
  left: 24px;
  z-index: 20;
  padding: 14px 16px;
  border-radius: 8px;
  background: rgba(0, 0, 0, 0.7);
  color: #ffffff;
  font-size: 14px;
  font-weight: 700;
  line-height: 1.4;
  text-align: center;
`;
